# Events API

## Endpoints

### GET /api/events
List all events.

### POST /api/events
Create new event (admin/moderator).

### GET /api/events/:id
Get event details.

### POST /api/events/:id/register
Register for event.

## Filters
- type, status, dateRange
