# Members API

## Endpoints

### GET /api/members
List all club members with pagination.

### GET /api/members/:id
Get member details by ID.

### PATCH /api/members/:id
Update member profile.

### DELETE /api/members/:id
Remove member (admin only).

## Query Parameters
- page, limit, role, status, search
