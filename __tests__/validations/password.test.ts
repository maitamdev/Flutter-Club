describe('Password Validation', () => { it('should require minimum length', () => { const isValid = (p: string) => p.length >= 8; expect(isValid('short')).toBe(false); expect(isValid('longpassword')).toBe(true); }); it('should require uppercase', () => { const re = /[A-Z]/; expect(re.test('Password1')).toBe(true); }); });
